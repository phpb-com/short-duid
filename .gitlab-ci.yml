iojs:
  image: iojs:latest
  script:
  - id
  - node -v
  - npm install --unsafe-perm
  - npm test

node11:
  image: node:0.11
  script:
  - id
  - node -v
  - npm install --unsafe-perm
  - npm test
  
node12:
  image: node:0.12
  script:
  - id
  - node -v
  - npm install --unsafe-perm
  - npm test